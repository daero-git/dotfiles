conky.config = {
    alignment = 'middle_middle',
    background = false,
    border_width = 0,
    default_color = 'c8d4bc',
    double_buffer = true,
    draw_borders = false,
    draw_graph_borders = false,
    draw_outline = false,
    draw_shades = false,
    use_xft = true,
    font = 'Noto Sans:size=9',
    gap_x = 0,
    gap_y = 0,
    -- Must match WINDOW_W/WINDOW_H in scripts/moon.py exactly -- that's
    -- what moon.py pads/centers the rendered diagram onto before saving,
    -- since ${image -p 0,0} below just anchors top-left with no centering
    -- of its own. Deliberately larger than the diagram's own natural size
    -- (which shifts whenever body/orbit constants are tuned) rather than
    -- an exact fit: previously an exact-but-stale minimum left the image
    -- invisible (window and file both fine, just size-mismatched), and
    -- extra margin is invisible anyway since the window background is
    -- fully transparent.
    minimum_height = 550,
    minimum_width = 1150,
    no_buffers = true,
    out_to_console = false,
    out_to_stderr = false,
    extra_newline = false,
    own_window = true,
    own_window_type = 'desktop',
    own_window_title = 'conky_moon',
    own_window_transparent = false,
    own_window_argb_visual = true,
    own_window_argb_value = 0,
    own_window_colour = '1c1c1c',
    own_window_hints = 'undecorated,below,sticky,skip_taskbar,skip_pager',
    stippled_borders = 0,
    update_interval = 1800.0,
    uppercase = false,
    use_spacer = 'none',
};

conky.text = [[
${execpi 1800 python3 /home/nick/.config/conky/scripts/moon.py}${image /home/nick/.config/conky/moon_snapshot.png -p 0,0}
]];
